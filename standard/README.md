# Standard

Standard holds the rules for applying canon to authored product surfaces.

Use standards to define how ContextKit writes runtime blocks, guides,
templates, validation checks, generated output, README prose, and command
surfaces.

Standards are more concrete than canon and less specific than a bundled file or
implementation detail.

