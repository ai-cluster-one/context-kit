# Assets Guide

`assets/` holds supporting project material, not always-on doctrine.

Use `sessions/` for session records, `plans/` for design plans, and `research/`
for evidence. Promote durable facts into the live layer that owns them.

ContextKit's own audit reports are not assets; `contextkit audit --write`
persists them under `.contextkit/audits/`.
