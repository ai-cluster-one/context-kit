# Templates

Templates are starter project-body files shipped by ContextKit.

They are gallery items, not doctrine for the ContextKit repository itself. A project can copy and adapt them during onboarding or initialization instead of inventing folder names and file shapes from scratch.

The manager reads this directory when listing templates or creating starter context files with template-enabled init, adopt, or bootstrap flows.
